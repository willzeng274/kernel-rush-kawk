Compiler completion does not authorize runtime use. Review both exact frozen cases before proposing any GPU run.

1. Check all input/artifact hashes and source-set identity. Native actual and expected records must match pointer/scalar signatures, runtime values, five producer or two merge constexprs, full specialization/divisibility indices, exact kwargs order/key, explicit options and every normalized parsed NVIDIA option. Both grids are 160. Triton is 3.1.0, target cuda/90/32 with sm_90a PTXAS, W4/S3, num_ctas=1, enable_fp_fusion=True, debug=None.
2. Producer must have one K loop, no persistent outer tile loop and no speculative next-tile shared writes. The four K intervals are [0,2432), [2432,4864), [4864,7296), [7296,9728). Each has 38 K64 steps. Last valid tile starts are 2368,4800,7232,9664. Inspect TTGIR and PTX prologue, every loop/stage rotation, all 38 iterations including epilogue, and every cp.async predicate/source-size. Source loads are unmasked: no global access may cross its tensor bound; compiler-generated prefetches outside the K interval must be absent or predicated safely. A zero source size still writes/zero-fills shared memory; global masking alone does not establish stage safety.
3. Trace every producer shared A/W stage and outstanding WGMMA group. Prove each operand's copy is complete and visible before matrix use, and no stage is rewritten/zero-filled while WGMMA can still read it. Prove final WGMMA and cp.async drains occur before shared reuse, output-store staging, shared deallocation and kernel return. The successful QKV V2 40-step trace is not a proof for this 38-step specialization; generic QKV V1 failed this condition.
4. Audit compiled address mapping for all 160 CTAs, 64 rows and 64 columns. For tile t, split=t%4, col=(t//4)*64+n and partial offset=(split*64+row)*2560+col. All 655360 FP32 partial elements must be in bounds and stored exactly once. Inputs are BF16, dot/partials FP32, no BF16 conversion before merge, no atomics, norm or residual path. Confirm physical matrix instruction shape m64n64k16 and correct K64 accumulation; do not infer arithmetic safety from the static form count alone.
5. Audit unchanged merge: 160*1024=163840 outputs, four FP32 plane loads, sequential FP32 additions from zero and one final BF16 conversion per result. All partial addresses lie in [0,655360), all output addresses [0,163840), each output stored once. Same-stream producer completion orders partial stores before merge reads and merge completion before the next retained normalization.
6. Record resources separately for producer and merge: registers/thread and CTA, shared bytes/layout, stack/spill bytes, matrix forms, async forms and all diagnostics. Reject nonzero stack/spills or unresolved bounds/stage-order concerns. Preserve TTIR, TTGIR, LLVM IR, PTX, cubin, verbose PTXAS diagnostics, configuration/metadata and artifact hashes. Compiler resources do not establish measured occupancy, BF16 model correctness or speed.
