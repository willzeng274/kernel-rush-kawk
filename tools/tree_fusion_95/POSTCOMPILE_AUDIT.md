Compiler completion does not authorize GPU or production use. Only three fused-kernel builds are proposed. The original reduction's launch metadata is verified but it is not compiled again.

1. Verify the full device module SHA256 `8e270daf605ad5f7daf019b6fca4b0a89ec3db6a7516154859e6850e5a3039d1`, host module SHA256 `a52bb5f452673005d7e7f04b379a8a820f848ddf13b93257b73cc100afbc2614`, exact91 attention/reducer and rope snapshots, all audit members, and all output hashes. Every one of six native launch records must match expected before the first build: argument order, pointer dtypes, scalar dtypes/values, eight fused constexprs/four reduction constexprs, specialization key, divisible-by-16/equal-to-one indices, exact explicit option insertion order, debug=None, normalized parsed options, grids and shapes. Use pinned Triton3.1 JIT/backend fingerprints, target cuda/90/32 and sm_90a PTXAS. Reduction source must remain byte-identical; no merge build is authorized here.
2. Reject any nonzero stack, spill load/store, or local load/store evidence. Record registers per thread/CTA, shared-memory bytes/layout, matrix forms, async copies/waits, casts, rsqrt/exponent instructions, barriers and branches. Preserve TTIR, TTGIR, LLVM IR, PTX, cubin, verbose PTXAS output, exact configuration/metadata and artifact hashes. Matrix instruction presence is only an inventory gate; validate actual operand types/shapes and FP32 accumulation. Neither occupancy nor speed follows from these resources.
3. Trace query input offsets `(b*R+t)*6144+(kh*4+g)*128+d`, its separate 64-element half reductions, add/rsqrt and every required BF16 cast boundary. Check reduction physical layout/order and exact Q head mapping after join `[64,GP,2]`, permute `[GP,2,64]`, ordered reshape `[GP,128]`. Inspect all shared conversions/temporary layouts. Current K repeats the same scalar-head formula one node at a time; key join/transposition must preserve half-major order. Confirm BF16 Q/K operands, BF16 rounded probabilities and raw BF16 V, with FP32 scores, stats and accumulators.
4. Prove compiled prefix/tail tile order and masks against the original S2 attention: start=s*384; end=min(start+384,pos+R); prefix_end=min(end,floor(pos/BN)*BN); prefix and tail advance by BN. Include zero prefix, aligned pos, mixed prefix/current tile, current nodes spanning a tile/split boundary, pos below the second split, and empty split. No extra softmax update may peel off current nodes. Query-tree masks preserve prefix visibility, signed right-shift semantics, ancestor/self bits and n<L. Verify all-masked rows retain (-inf,0,0) without NaNs and empty splits still publish the original partial layout.
5. Audit every actual cache-read instruction/predicate: only n<end AND n<pos may read K/V. Masking scores later is insufficient. Current K/V must derive from immutable QKV, irrespective of cache-store scheduling; invalid lanes must not read negative/out-of-bound QKV addresses. Current K guards use slot=pos+j, rotary=pos+depth[j], and n0<=slot<min(n0+BN,end). Check every R4/R8 guard, sibling depths and both halves. Current V lane uses j=n-pos and `(HQ+HKV+kh)*128+d`; owning current-mask lanes publish V. Prove each `(b,kh,j,d)` cache slot is written exactly once across the two splits and tile intervals, never omitted, never consumed by another CTA in this launch. Validate all cache/QKV/table/partial addresses under documented dynamic value invariants.
6. Inspect prefix and tail compiler pipelines separately. Trace K/V copy completion before matrix use; inspect every cp.async predicate/source-size and each shared-memory stage/descriptor. A zero source size still clears shared storage, so global read masking does not prove stage safety. Prove no stage is overwritten while an outstanding matrix group can consume it, including current-node branch merges, layout conversions, loop epilogues, partial-store staging, and return. All WGMMA/copy work must drain before conflicting shared reuse/deallocation. Check both branch outcomes for every unrolled node and empty-loop paths. Unresolved staging or register-lifetime concerns block runtime.
7. Confirm output partial offsets `(((b*32+head)*R+t)*2+s)*128+d`, corresponding max/sum offsets, and complete unique stores. The unchanged reduction consumes these same owner buffers on one stream after the fused launch. Only after source/resource/layout/pipeline audits may a separate authorization consider numerical cache/output comparison, exact-greedy validation, and full-layer-rotation timings against the incumbent's separate norm/cache plus attention path. Keep incumbent BN/W/S and actual selected two-split geometry unchanged.
