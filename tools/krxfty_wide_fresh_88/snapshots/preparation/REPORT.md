The M64 projection candidate is frozen at `work/candidates/krxfty_wide64/engine`, based on exact candidate 83. It changes one existing file, `model.py`, and adds `kernels/m64_matmul_selection.py`; the other 17 existing files are byte-identical. The hook replaces only QKV, attention output, MLP down, and LM projection calls at M=64. Existing narrow kernels, M64 gate/up selection, strict greedy acceptance, pacing, self-checks, and generation drains remain intact. No compiler, GPU, CI, main-tree, push, or submission activity occurred.

Prefer this bounded integration to immediately reproducing full public 92d6da7. That public baseline changes seven engine files versus reproduced 7f0f98c, including drafting, pacing and probes. The author associates run 8263784b and 1221.4 with wider tiles in [f948543](https://github.com/IshaanBansal2006/starter/commit/f948543ef5b724ada6a16d19a91d4df57d7c1466). The exact source revision of that run remains unidentified. [92d6da7](https://github.com/IshaanBansal2006/starter/commit/92d6da7f8b4a03c7a35796162bd8816e03d9a1c6) contains the first two new configurations; it is a source comparison point, not a proven 1221.4 artifact. [fc0c6e7](https://github.com/IshaanBansal2006/starter/commit/fc0c6e7fb54ebf28e2380b50b522a69ad460375f) restores exact acceptance after margin 0.6 lost 1166 versus 1185.

Full AGENTS, contract and guide were read. Fresh [live Docs](https://htn.dryft.ai/docs) supersede their stale workload count: six hidden plus three public cases, five samples each, all runs official. Load plus warmup has 300 seconds; each sample has 300 seconds. The model remains pinned BF16 Qwen3-4B-Instruct-2507, revision cdbee75f, on H100 with Triton 3.1.0.

The existing single-M-tile SkinnyMatmul already accepts a BLOCK_M constant. A separate wrapper sets it to 64; every existing GEMM device function stays unchanged. Standard tree batches 1/2/4/8/16 produce 64 verifier rows. Other shapes retain their existing routes. QKV and LM retain standalone add_rms_norm, preserving residual ownership and all norm casts.

Three fixed configurations are enabled: A is BN64/BK64/SK1/W8/S3/P1; B is BN128/BK64/SK1/W8/S4/P1; C is BN128/BK64/SK2/W8/S3/P2. At 132 SMs, their grids are QKV 96/48/96, output 40/20/40, down 40/20/40, LM 132/132/264. C doubles B's small-projection block count and halves each K interval, but adds a reduction and FP32 partial traffic. Its LM partial allocation is 74.1875 MiB. All dot accumulation and split reduction remain FP32, followed by one BF16 output cast. No partial is rounded to BF16. Gate/up arithmetic is unchanged.

Resource estimates remain hypotheses: accumulator floors are 16/32/32 FP32 registers per thread at 256 threads; logical stage operand budgets are 48/96/72 KiB before compiler scheduling and scratch. [Hopper limits](https://docs.nvidia.com/cuda/hopper-tuning-guide/index.html#occupancy) are 228 KiB shared memory per SM, 227 KiB per block and 64K registers per SM. Actual WGMMA selection, pipelining, shared allocation, barriers and spills require compilation.

Whole-call screening uses all 36 actual layer matrices and their corresponding norm weights, or the single LM matrix. It checks finite pointwise error ≤ 0.02|reference|+0.001, exact residual output, and exact LM argmax. Two opposite-order full-weight-rotation measurements must both beat the original by more than 3%. Selection starts with at least 42 seconds remaining in the existing 120-second budget, which starts before model loading; one 18-second admission window preserves a 24-second reserve. Running calls cannot be preempted. Any incomplete suite returns all original callables. An owned timer retains asynchronous owners; failed drains quarantine graph, stream, input, output and partial owners and raise a fatal BaseException that bypasses existing ordinary fallback handlers. No subprocess probe, shared cache or self-check bypass was copied.

CPU checks pass 24 policy/lifetime cases and all 12 exact host launches, including four split reductions. They exercise real host launchers with mocked device calls; they do not validate BF16 device arithmetic or speed. Runnable `derive_native_keys.py` executes the pinned Triton binder and specialization logic on CPU. Its source is byte-identical to freshly fetched [Triton 3.1](https://github.com/triton-lang/triton/blob/v3.1.0/python/triton/runtime/jit.py). The 16 launch records reduce to seven native keys: QKV versus output/down/LM for each A/B/C, plus one sum kernel. `COMPILE_PLAN.json` requires native-worker key revalidation before exactly those seven builds, one bounded worker, no retries or search expansion. Runtime correctness and throughput remain unmeasured.
