Recommend the bounded M64 integration on candidate83, then a complete official comparison if it survives validation. Full public92d6da7 is a useful control, but not the first next candidate: it changes seven engine files versus reproduced7f0f98c (642 additions/147 deletions), including drafting, pacing, acceptance machinery and probes. The author links run8263784b/1221.4 to wider tiles in [f948543](https://github.com/IshaanBansal2006/starter/commit/f948543ef5b724ada6a16d19a91d4df57d7c1466); neither that message nor adjacent commits identifies its exact source revision. [92d6da7](https://github.com/IshaanBansal2006/starter/commit/92d6da7f8b4a03c7a35796162bd8816e03d9a1c6) is the closest source containing the first two new configurations, not a proven1221.4 artifact. Author [fc0c6e7](https://github.com/IshaanBansal2006/starter/commit/fc0c6e7fb54ebf28e2380b50b522a69ad460375f) restores exact acceptance after margin0.6 lost1166 versus1185. Keep strict greedy.

Read full AGENTS, contract and guide. Fresh [live Docs](https://htn.dryft.ai/docs) supersede their old workload count: six hidden plus three public cases, five samples each, every run official; 300s load+warmup, 300s/sample. Target remains pinned BF16 Qwen3-4B-Instruct-2507/cdbee75f, H100, Triton3.1.0. No compiler, GPU, CI, main-tree or submission activity occurred.

The exact83 skinny device kernel already handles one constexpr BLOCK_M tile. A separate wrapper can set BLOCK_M=64 while leaving gemm.py byte-identical. This avoids the public generic-M rewrite, which moves norm statistics inside every persistent tile. Keep standalone add_rms_norm for QKV/LM; preserve existing M64 gate/up selection, narrow pickers, self-check, graph lifecycle and drains. A single hook after existing default decode_matmuls selection replaces only QKV/O/down/LM when M=64. Standard tree batches1/2/4/8/16 produce64 verifier rows; off-shapes retain existing routes.

Two primary configurations: A=BN64/BK64/SK1/W8/S3/P1; B=BN128/BK64/SK1/W8/S4/P1. Optional C is the author's second new configuration: BN128/BK64/SK2/W8/S3/P2. At132 SMs their grids are QKV96/48/96, O40/20/40, down40/20/40, LM132/132/264. C restores A's low-N CTA count while halving each CTA's K interval; it adds a reduction launch and FP32 partial traffic. LM partial storage is74.1875MiB; QKV3MiB, O/down1.25MiB each. Thus C is a plausible optional third, particularly for occupancy, not an established win.

All use one64-row tile, FP32 dot accumulation, original BF16 weights and one BF16 output rounding. Split2 stores FP32 partials and sums FP32 before that cast; it must never round partials to BF16. No norm fusion means residual writes/cast positions are exactly original; xout stays distinct from inputs and is written only by original add_rms_norm. Gate/up's BF16 gate/up/SiLU/product boundaries are unchanged.

Resource estimates are hypotheses: A/B/C accumulator floors16/32/32 FP32 registers per thread at256 threads; logical stage operand budgets48/96/72KiB before compiler scheduling/scratch. [Hopper limits](https://docs.nvidia.com/cuda/hopper-tuning-guide/index.html#occupancy) are228KiB shared/SM,227KiB/block,64K registers/SM. Actual WGMMA choice, shared allocation, pointer/layout overhead, barriers and spills require compilation. M64 is the compiler path the upstream150s child probe was trying to protect; compiling is not execution validation.

Minimum proposed compile plan: eight exact main launches (two configs × four shapes:64×6144×2560,64×2560×4096,64×2560×9728,64×151936×2560), deduplicated only after native Triton specialization keys prove equivalence. Optional C adds four main launches plus the existing sum kernel's four exact output lengths. Record native signature/attributes, TTIR/TTGIR/LLIR/PTX, register/shared/spill counts and all diagnostics; inspect WGMMA/async copies, bounds and output ownership. One bounded worker, no retries or sweep. No compile dispatch is proposed before root chooses configs.

Runtime proposal compares whole callable versus untouched83 across all36 actual layer weights (one LM matrix), finite pointwise error≤0.02|reference|+0.001, exact residual output, and LM argmax equality. Require two opposite-order full-weight-rotation timings both ≥3% faster. A shared18s admission window starts only with≥42s remaining in existing120s budget (started before load), preserving≥24s reserve; calls already running cannot be preempted. Incomplete selection returns all original callables. Propagate unexpected/CUDA failures; no sticky-error swallowing, subprocess probe, shared cache, or self-check bypass. These checks are screening, not exact-output proof; runtime correctness and score remain unknown.
