Read-only independent review: frozen exact-top8 candidate

Verdict: no concrete source-level blocker found. Suitable for the separately authorized compile audit; compilation, real-device correctness, capture/replay behavior, load/warmup duration, and performance remain unproved. This is not approval to replace #82 or submit. Current official-only, six-hidden-plus-three-public rules override the stale included workload/workflow text. No candidate edits, test reruns, compilation, CI, GPU, main-branch changes, or submissions occurred.

Provenance: independently hashed all 16 candidate files and compared every existing file directly with Git commit c3e4bfd6c65585c53e8552743f50f7285effee6d. Only model.py and recycle.py differ; kernels/top8.py is added. All manifest artifact hashes and four device-function ASTs match. Kernel SHA is 3a8fdbd8340e9970ddaab1ae3be5131181ae7900d4e5b3362114f5bf255d4e3b; source-lines SHA is 518b2d24b54f19158bb86b5d275703237866d3d8c9b017441f01b81509a21b5c. The frozen candidate contains neither #81 seeding nor #82 M64 files nor pair-cache changes.

Correctness: top8.py:26–66 applies the local PyTorch 2.5.1 SortingRadixSelect.cuh BF16 transform, adding one so no valid encoding shares invalid/deleted key zero. NaNs collapse to highest key; positive zero precedes negative zero; infinities remain valid. Pair reduction is an associative lexicographic maximum with lowest global ID breaking equal keys. Each 2048-value partition has at least 384 valid entries, so eight reductions cannot exhaust valid entries. Every global top-eight element belongs to its partition's top eight; merging the 600 retained candidates is exact under this ordering. Selected-ID deletion prevents duplicates, including all-equal/infinite/NaN rows; merge padding cannot win. Equal-valued proposal IDs/order may differ from unstable torch topk. Duplicate destination tokens retain the original index_copy_ behavior, whose CUDA winner ordering the CPU tests do not establish.

Integration/ownership: model.py:373 changes only verifier update dispatch; :374 keeps authoritative logits.argmax. Recycler.update, Plan.prefill, and model-based _warm_table remain original, including identical-row-count prefill. Original table initialization/lifecycle and graph/request cleanup remain unchanged. Each Recycler owns disjoint, stable keys/IDs/output storage, fully overwritten per invocation (4832N bytes). Selected replay has no allocation or host choice in the new helper. Logits remain the existing contiguous BF16 output.

Picker: top8.py:113–197 validates input bits, IDs, uniqueness, finite logits and selected values before timing. Both timed graphs include complete top-k/index conversion/scatter work. ABBA observations require every candidate observation to beat every baseline observation by 5%; a final real update follows selection. Repeated cached logits and duplicate-token scatter mean this does not demonstrate full-round improvement. Low budget, first-use capture, validation rejection, or deadline exhaustion stick with torch. Unknown errors poison the owner and propagate after drain; successful drains release trial graphs/inputs, failed drains quarantine the owner and retain those references. The 19 checkpoints are checked deadlines, not preemption guarantees.

#82 composition: pick_m64_gateup runs during plan/VerifyPlan matrix-picker construction, before GraphPlan._warm_eager reaches the first verifier update. Both share the 120-second deadline started before model loading. M64 can consume its checked window before top8's admission check. Top8 starts only with at least 42 seconds remaining, admits 12 seconds locally, and checks a 30-second reserve. Consequently a combined candidate may retain torch top8; neither path selection nor performance follows from #82's official result. The three exact #82 kernel files should be reviewed as a distinct combined source freeze; pair-cache remains separate.

Evidence limits: the CPU harness executes actual AST bodies with checked pointers and the actual pair combiner; its numeric/NaN/signed-zero oracle is independent of the bit formula. Seven meaningful mutations test addressing, ranking and deletion. CUDA graphs/events are mocks and layer logits are injected. CPU_RESULTS.log records the earlier 57abae… host-source hash; final JSON records this freeze and asserts a quarantine-only host rerun without a separate final transcript. Compile cases match runtime constants, grids/options and expected pointer/constexpr alignment attributes; N changes only grids. The compile harness checks frozen-device hashes/AST, but does not itself re-read current engine source, so preserve this verified freeze when invoking it.
