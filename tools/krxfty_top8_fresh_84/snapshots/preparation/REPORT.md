The isolated candidate is ready for review, not submission. It starts from exact winning #80 (`c3e4bfd6c65585c53e8552743f50f7285effee6d`). Only `recycle.py` and one verifier call in `model.py` change; `kernels/top8.py` is added. The other 13 engine files remain byte-identical. #81 prompt-successor seeding, WGMMA, pair-cache work and their lifecycle changes are absent.

`Recycler.update_verifier` explicitly selects the new helper when k=8/V=151936 were configured. `Recycler.update` remains the original torch operation, so prompt seeding stays original even when its row count coincides with verification. Other k values retain torch. Tree geometry, model arithmetic, full logits, `index_copy_`, and the separate authoritative `logits.argmax(...).view(B,R)` are unchanged. Original #80 table lifecycle is preserved.

The fixed two-kernel design extends historical `recycling_topk_fresh` k=2/k=4 partition/merge work; no novel-algorithm claim is made. Kernel1 loads 75 tiles of 2048 BF16 values per row, with 384 valid entries in the final tile. It retains eight winners per tile. Kernel2 selects eight from 600 candidates padded to 1024. A global top-eight item must occur in its tile’s top eight. Both use eight lexicographic pair reductions, four warps, three stages, no floating-point fusion.

The integer key follows PyTorch 2.5.1 BF16 radix ordering: negative values reverse their bits, positives flip the sign bit, NaNs rank highest, and +0 precedes -0. Adding one reserves zero exclusively for masked/deleted lanes. Keys fit int32; equal keys choose the smallest global token ID. Removing a selected ID prevents duplicates on equal/infinite rows. These sorted proposals can differ from torch’s unstable tied-index ordering, affecting acceptance/timing. They never replace the original greedy result.

Each recycler owns disjoint int32 keys/IDs `[N,75,8]` and output `[N,8]`, allocated before capture; N=B×R. Every entry is overwritten each invocation, including repeated graph replay. Persistent scratch/output costs 4832N bytes:309,248 at N=64; 927,744 at N=192. No counters, resets, dynamic allocation or host decisions occur inside selected replay. Actual logits are contiguous BF16 from the unchanged model output.

On the first eager verifier update, a sticky warmup choice starts only with at least 42 seconds remaining in the existing 120-second shared picker budget. It allows 12 seconds locally while preserving 30 seconds of shared budget; 19 checks surround validation, captures and timed observations. A single compilation or synchronization cannot be preempted, so this is a checked deadline, not a hard runtime guarantee. Insufficient budget or first use during capture keeps torch permanently.

Validation snapshots logits and checks raw BF16 bits unchanged after the custom launch, in-range/distinct IDs, finite model logits, and exact selected values against torch topk. Bad ranks/IDs/nonfinite inputs retain torch. Two temporary graphs each contain four complete updates, including the unchanged scatter; ABBA event observations replay eight updates each. Selection requires every candidate observation to beat every baseline observation by 5%. Slower results and deadline exhaustion keep torch. The final actual update still executes after this comparison.

Temporary graph owners and their external table/token/logit/snapshot inputs are retained until successful synchronization. Unknown compilation/device/capture errors propagate after draining; failed trials poison the helper against reuse. A failed drain quarantines the owner in a module list until process exit, preserving graph/input references even if the parent abandons its plan. Existing plan synchronization and request cleanup remain unchanged. Validation adds a temporary full-logit snapshot (19.45MB at N=64), plus temporary validation/native-topk allocations. Trial graph memory is released after a successful drain.

CPU evidence uses PyTorch 2.5.1 and executes the actual device function bodies with checked pointers, a balanced reduction calling the actual pair combiner, and BF16 bitcast emulation. An independent numeric/NaN/signed-zero ordering oracle checks all 65,536 encodings. Eleven kernel cases cover 272 rows, including N=64/N=192, dense ties, infinities/NaNs, final tiles, vocabulary endpoints, poisoned-buffer overwrite and unchanged logits/greedy. Seven intentional mutations are detected. Thirty-three selector tests cover timing decisions, all 19 budget checkpoints, validation failures, input mutation, compilation errors, failed-drain retention and sticky rejection. Fifteen integration/launch checks execute the actual verifier with injected logits, exercise selected top8 through scatter, confirm identical-shape prefill stays original, and verify unchanged greedy/prefill source plus runtime launch arguments.

`device_top8.py` contains exactly the four frozen device functions with AST parity to the engine. `compile_top8.py` prepares exactly two Triton 3.1 SM90 cases: partials `(*bf16,*i32,*i32;151936,75,2048)` and merge `(*i32,*i32,*i32;75,1024)`, with actual alignment attributes and launch options. N affects only the launch grid. It checks hashes/AST before compiling and collecting assembler resource evidence. No compiler, CI, GPU, main branch or official action was run.

Compilation, real-device correctness, warmup duration and performance remain unproven. Repeated-update timing can favor cached logits; full-round and official results remain necessary. Eight serial reductions may outweigh saved radix launches/reads. Retain #80 until those checks establish a gain.
